# ========
#   Hyprland config file for my "diinki aero" rice. Hyprland is the right tool for the job due to all of its available
#   window effects, but you could achieve 99% of the same design with swayfx as well; I just chose hyprland due to
#   compatability issues with nvidia GPU.
# ========


#                                    !!MONITORS!!                                   #
# --------------------------------------------------------------------------------- #
#                                    !!MONITORS!!                                   #

# Make sure to customize monitors! This is just my setup so you get an idea.
# run `hyprctl monitors all` to see all your monitors!!!
monitor=,preferred,auto,1
#exec-once = ~/dotfiles/hypr/scripts/CheckScreens.sh

# Startup programs, these are mine, most likely you'll have these as well.
exec-once = hyprpaper
exec = $HOME/.config/eww/scripts/start.sh


# Start Polkit
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = waybar

# Load Dunst Notification Manager
#exec-once = dunst
exec = swaync

# Load GTK settings
exec-once = ~/dotfiles/gtk/gtk.sh

# Load swayidle and define the lock screen time
# Using hypridle to start hyprlock
exec-once = hypridle

#exec-once = ~/dotfiles/hypr/scripts/lockscreentime.sh

# Load cliphist history
exec-once = wl-paste --watch cliphist store

# Initialize swww
exec-once = swww init || swww-daemon --format xrgb




# Environment variables
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24



#                                    !!INPUT!!                                      #
# --------------------------------------------------------------------------------- #
#                                    !!INPUT!!                                      #

# READ https://wiki.hyprland.org/Configuring/Variables/#input IF CONFUSED!
# Example keyboard/mouse input settings.
input {
    # English, Swedish, and Spanish keyboard layout example,
    # Switch layout with pressing: alt + shift
    kb_layout = gb
    kb_options = 

    # Set as needed
    kb_rules = 
    kb_variant =
    kb_model =

    follow_mouse = 1

    # Range is -1.0 to 1.0 | 0 means no modification to sensitivity.
    sensitivity = 0 

    touchpad {
        natural_scroll = false
    }
}

# This fixes a few bugs.
cursor {
  no_hardware_cursors = true
}


#                                    !!DESIGN!!                                     #
# --------------------------------------------------------------------------------- #
#                                    !!DESIGN!!                                     #

# I prefer quick animations, that's what this setup is.
animations {
    # SET enabled = false if you want to disable animations.
    enabled = true

    bezier = myBezier, 0, 1, 0.18, 1.0

    animation = windows, 1, 1.5, myBezier
    animation = windowsOut, 1, 2, myBezier, popin 95%
    animation = border, 1, 12, myBezier
    animation = borderangle, 1, 5, default
    animation = fade, 1, 6, default
    animation = workspaces, 1, 6, default
}

# The gaps between windows, as well as border colors.
# proportional to the taskbar values.
general {
    # Inner and Outer gaps between windows.
    gaps_in = 5
    gaps_out = 10

    # I prefer a thin border
    border_size = 1

    # Border colors.
    col.active_border = rgb(18,18,18)
    col.inactive_border = rgb(18,18,18)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    layout = dwindle

    # READ https://wiki.hyprland.org/Configuring/Tearing/ BEFORE TURNING ON!
    allow_tearing = false
}

# Window Decorations! Shadow, Blur, etc.
decoration {
    # 8px same as taskbar, change if wanted.
    rounding = 12

    # I want transparancy to not change, since we have the colored border.
    active_opacity = 1.0
    inactive_opacity = 1

    # Window Shadow
    shadow:enabled = true
    shadow:range = 16
    shadow:render_power = 5
    shadow:color = rgba(0,0,0,0.35)

    # Transparent Window Blur
    blur:enabled = true
    blur:new_optimizations = true
    blur:size = 2
    blur:passes = 3
    blur:vibrancy = 0.1696
}

# Read https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more info on dwindle layout.
dwindle {
    pseudotile = true
    preserve_split = true
}

# Read https://wiki.hyprland.org/Configuring/Master-Layout/ for more info on master layout.
master {
    new_status = master
}

# We have our own wallpapers :) So I'll disable default things.
misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}
# No need for gestures unless you have a touch display.
gestures {
    workspace_swipe = false
}


#                                    !!KEYBINDS!!                                   #
# --------------------------------------------------------------------------------- #
#                                    !!KEYBINDS!!                                   #
# ----------------------------------------------------- 
# Key bindings
# ----------------------------------------------------- 

# SUPER KEY
$mainMod = SUPER

# Applications
bind = $mainMod, RETURN, exec, kitty
bind = $mainMod, B, exec, firefox
bind = $mainMod, S, exec, spotify-launcher
bind = $mainMod ,space, exec, pgrep wofi > /dev/null 2>&1 && killall wofi || wofi -S drun -c ~/dotfiles/wofi/config -s ~/dotfiles/wofi/style.css
# Windows
bind = $mainMod, Q, killactive
bind = $mainMod, F, fullscreen
bind = $mainMod, E, exec, ~/dotfiles/scripts/filemanager.sh
bind = $mainMod, T, togglefloating
bind = $mainMod SHIFT, T, exec, ~/dotfiles/hypr/scripts/toggleallfloat.sh
bind = $mainMod, J, togglesplit
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind = $mainMod SHIFT, right, resizeactive, 100 0
bind = $mainMod SHIFT, left, resizeactive, -100 0
bind = $mainMod SHIFT, up, resizeactive, 0 -100
bind = $mainMod SHIFT, down, resizeactive, 0 100

# Actions
bind = $mainMod, N, exec, swaync-client -t
bind = $mainMod, R, exec, hyprshot --mode region --output-folder ~/Pictures/screenshots/
bind = CTRL ALT, 0xffff, exec, wlogout #control alt delete
bind = $mainMod CTRL, H, exec, ~/dotfiles/hypr/scripts/keybindings.sh
bind = $mainMod SHIFT, B, exec, ~/dotfiles/waybar/launch.sh
bind = $mainMod CTRL, B, exec, ~/dotfiles/scripts/restarteww.sh
bind = $mainMod SHIFT, R, exec, ~/dotfiles/hypr/scripts/loadconfig.sh
bind = $mainMod CTRL, F, exec, ~/dotfiles/scripts/filemanager.sh
bind = $mainMod CTRL, C, exec, ~/dotfiles/scripts/cliphist.sh
bind = $mainMod, V, exec, ~/dotfiles/scripts/cliphist.sh
bind = $mainMod CTRL, T, exec, ~/dotfiles/waybar/themeswitcher.sh
#bind = $mainMod CTRL SHIFT, A,exec,  ~/dotfiles/hypr/scripts/ToggleLaptopScreen.sh
bind = CTRL ALT, space, exec, playerctl play-pause
bind = CTRL ALT, left, exec, playerctl previous
bind = CTRL ALT, right, exec, playerctl next
bind = ,KP_Up, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = ,KP_Down, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = ALT SHIFT CTRL, S, exec,  ~/dotfiles/scripts/sleep.sh
bind = $mainMod, f2, exec, brightnessctl -q s 0%
bind = $mainMod, f3, exec, brightnessctl -q s 50%
bind = $mainMod SHIFT, f3, exec, brightnessctl -q s 100%
bind = $mainMod, L, exec, hyprlock

# Workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1
bind = $mainMod CTRL, down, workspace, empty
bind = $mainMod CTRL,1 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 1
bind = $mainMod CTRL,2 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 2
bind = $mainMod CTRL,3 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 3
bind = $mainMod CTRL,4 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 4
bind = $mainMod CTRL,5 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 5
bind = $mainMod CTRL,6 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 6
bind = $mainMod CTRL,7 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 7 
bind = $mainMod CTRL,8 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 8
bind = $mainMod CTRL,9 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 9 
bind = $mainMod CTRL,0 , exec, python ~/dotfiles/hypr/scripts/swapWindows.py 10



# Fn keys
bind = , XF86MonBrightnessUp, exec, brightnessctl -q s +10%
bind = , XF86MonBrightnessDown, exec, brightnessctl -q s 10%-
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioPause, exec, playerctl pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous
bind = , XF86AudioMicMute, exec, pactl set-sink-mute @DEFAULT_SOURCE@ toggle




# Passthrough SUPER KEY to Virtual Machine
bind = $mainMod, P, submap, passthru
submap = passthru
bind = SUPER, Escape, submap, reset
submap = reset


#                                    !!WINDOW RULES!!                               #
# --------------------------------------------------------------------------------- #
#                                    !!WINDOW RULES!!                               #


# -- Some of my personally preferred window rules, you will probably like these as well! ---%
# -- There's also lots of bug-fixes for specific apps here.                              ---%

# Read https://wiki.hyprland.org/Configuring/Window-Rules/ for more info
# Read https://wiki.hyprland.org/Configuring/Workspace-Rules/  for more info


# This adds a blur to the background of wofi (our app launcher)
# The reason we can't do this within wofi itself, is because gtk3 doesn't
# have a built-in background blur. 
layerrule = blur, wofi 
layerrule = ignorealpha 0.01, wofi # This is so entirely transparent things aren't blurred.

# Add blur to waybar, for same reason as above. Since we have two versions
# of the waybar design, one with translucency.
layerrule = blur, waybar
layerrule = blurpopups, waybar
layerrule = ignorealpha 0.01, waybar # this is so entirely transparent things aren't blurred.

# Add blur to eww widgets
layerrule = blur, eww
layerrule = blurpopups, eww
layerrule = ignorealpha 0.01, eww

# Fix dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0




windowrule = tile,title:chromium
windowrule = float,title:blueman-manager
windowrule = float,title:nm-connection-editor
windowrule = float,title:qalculate-gtk
windowrule = float,title:rofi
windowrulev2 = stayfocused, class:(rofi)
